# --- Firebase Client SDK (public) ---
# Copy to .env.local and fill in from Firebase project settings > General > Your apps
NEXT_PUBLIC_FIREBASE_API_KEY=your_api_key
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=your_project.firebaseapp.com
NEXT_PUBLIC_FIREBASE_PROJECT_ID=your_project_id
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=your_project.appspot.com
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=1234567890
NEXT_PUBLIC_FIREBASE_APP_ID=your:app:id
# Optional (if using analytics / measurement)
# NEXT_PUBLIC_FIREBASE_MEASUREMENT_ID=G-XXXXXXX

# --- Firebase Admin SDK (server only) ---
# Provide either a service account JSON (preferred) or individual fields.
# If using JSON, paste the JSON (single line) into FIREBASE_ADMIN_SERVICE_ACCOUNT_JSON.
# Otherwise fill individual vars. Do NOT commit real values.
FIREBASE_ADMIN_PROJECT_ID=your_project_id
FIREBASE_ADMIN_CLIENT_EMAIL=firebase-adminsdk-xxxxx@your_project_id.iam.gserviceaccount.com
FIREBASE_ADMIN_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\\nLINE1\\nLINE2\\n-----END PRIVATE KEY-----\\n"
# Alternatively: FIREBASE_ADMIN_SERVICE_ACCOUNT_JSON='{"type":"service_account",...}'

# --- Application Config ---
# Default seed user email for local seeding (must exist or will be created via Auth UI)
SEED_USER_EMAIL=developer@example.com
# Optional locale override
DEFAULT_LOCALE=en
